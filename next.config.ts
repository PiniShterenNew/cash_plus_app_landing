import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Recommended in production: add these for better performance:
  // compress: true,
  // poweredByHeader: false,
};

export default nextConfig;
